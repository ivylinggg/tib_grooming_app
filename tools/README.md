# Training history photo import

This directory contains local, one-time import utilities. Do not commit Firebase service-account credentials or generated employee photos here.

The historical training records are already stored in Firestore under `training_history`. Photo URLs should be populated by a local script using the service-account key stored outside the repository.
